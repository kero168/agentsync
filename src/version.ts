// Keep in sync with the "version" field in package.json.
// (Not imported from package.json to avoid JSON-import/ESM interop
// differences across supported Node versions and bundlers.)
export const VERSION = '0.1.0';
