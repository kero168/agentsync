# internal-tools

Shared guidelines for the internal-tools monorepo.

<!--
  This file is generated by agentsync for the AGENTS.md standard.
  Do NOT edit it directly — your changes will be overwritten.
  Edit the sources instead: agentsync.config.yaml and the fragments in rules/,
  then regenerate with `agentsync build` (or verify with `agentsync check`).
-->

## Overview

internal-tools is a monorepo of small internal services. This file is
listed first via `rulesOrder`; every other fragment in `guidelines/` is
appended afterwards in alphabetical order.

## Security

- Never log secrets, tokens, or customer PII, even at debug level.
- All internal services require authentication; there is no "trusted network" exception.
